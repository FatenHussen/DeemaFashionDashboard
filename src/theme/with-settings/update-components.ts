// This file is no longer needed since we're not using MUI components
// Keeping it as a stub for backwards compatibility

export function applySettingsToComponents() {
  // No-op: Component overrides are handled via CSS variables and Tailwind classes
  return {};
}
